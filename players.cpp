#include <iostream>
#include <unistd.h>
using namespace std;

int main() {
    int player1, choice = 0;
    bool flag = false;
    if (!flag) {
        cout << "Ход игрока " << player << ' ';
        cin >> ws >> choice;
        ++player;
        if (choice == 10) { return 0; }
        cout << "Ход игрока " << player << ' ';
        cin >> ws >> choice;
        main()
    }
    return 0;
}